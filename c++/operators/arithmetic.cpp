//operators are used to perform operations on variables and values.

#include <iostream>
using namespace std;

int main(){
    int sum1 = 10;
    int sum2 = 5;
    int sum3 = sum1 + sum2;

    cout << sum3 % 6;
    return 0;
}

/*

Addition (adds together two values): + . ex: x + y
Subtraction (subtracts one value from another): - . ex: x - y
Multiplication (multiplies two values): * . ex: x * y
Division (divides one value by another): / . ex: x / y
Modulus (returns the divison remainder): % . ex: x % y
Increment (Increases the value of a variable by 1): ++ . ex: ++x
Decrement (Decreases the value of a variable by 1): -- . ex: --x

*/